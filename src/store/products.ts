export { createProduct, updateProduct, removeProduct, fetchProducts, selectProducts, selectProductsLoading, selectProductsError, toggleStatus } from "@/features/products/productsSlice"
export { type Product } from "@/features/products/productsApi"
export { default } from "@/features/products/productsSlice"
